1. inbuilt data structure
2. Linear data structure

Array a. fixed size
      b. primitive data types can be stored
ArrayList a. dynamic size
          b. primitive data types cannot be stored directly

1. Add Element O(1)
2. Get Element O(1)
3. Remove Element O(n)
4. Set Element at Index O(n)
5. Contains Element O(n)

Size of ArrayList .size()